# OS files
.DS_Store
Thumbs.db

# Raw data files (large / sensitive)
*.ps1

# Ignore loose CSVs in root but NOT the master data/ folder
*.csv
!data/*.csv

# Secrets — never commit these
.env
.env.local
.env*.local

# Node
node_modules/

# Local Netlify folder
.netlify
